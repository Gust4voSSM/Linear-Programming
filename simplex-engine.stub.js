/*
 * Raw Simplex engine stub.
 *
 * Keep this file as a clean starting point for implementing the algorithm
 * without UI concerns. The active testing implementation remains in
 * simplex-engine.js.
 */
function createSimplexIterations(context) {
  return [context.initialFrame];
}
